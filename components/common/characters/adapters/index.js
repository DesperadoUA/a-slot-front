import DefaultAdapter from "./default"
import CommaSeparationAdapter from './commaSeparation' 

const Adapters = {
   default: DefaultAdapter,
   commaSeparation: CommaSeparationAdapter
}

export default Adapters